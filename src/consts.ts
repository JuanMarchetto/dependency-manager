export const errorMessage = 'Error: Invalid input';
export const commadNames = ['DEPEND', 'INSTALL', 'REMOVE', 'LIST'];
export enum status {
    NOT_INSTALLED,
    IMPLICITLY_INSTALLED,
    INSTALLED,
}
